# Dependencies for the DocIntel platform: FastAPI API, worker and test suite.
#
#   pip install -r requirements-platform.txt
#
# This includes requirements.txt, because the platform reuses the prototype's
# pure-logic modules (security_analyzer, advanced_features, pdf_processor).
-r requirements.txt

# ---------------------------------------------------------------- platform
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
alembic>=1.13
pydantic-settings>=2.4
email-validator>=2.2
python-multipart>=0.0.9

# ------------------------------------------------------------------- auth
# bcrypt is used directly; passlib is deliberately NOT used, as passlib 1.7.4
# is incompatible with bcrypt 4.x/5.x and is effectively unmaintained.
bcrypt>=4.2
pyjwt>=2.9

# -------------------------------------------------------------------- pdf
# Licences checked for commercial distribution:
#   pypdf      BSD-3-Clause          page ops, forms, composition
#   pikepdf    MPL-2.0               AES-256 encryption, compression (QPDF)
#   pypdfium2  BSD-3/Apache-2.0      rasterisation (PDFium)
#   reportlab  BSD                   overlays and generated documents
#   pillow     MIT-CMU               image encoding
#   cryptography  Apache-2.0/BSD-3   AES for reading protected PDFs
#
# PyMuPDF/fitz is intentionally excluded: it is AGPL-3.0, which would require
# publishing the source of this networked application or buying a commercial
# licence from Artifex.
pikepdf>=9.0
pypdfium2>=4.30
reportlab>=4.2
pillow>=10.0
cryptography>=43

# --------------------------------------------------------------------- ai
langchain>=0.0.350

# ------------------------------------------------------------------ shared
requests>=2.28.0

# -------------------------------------------------------------------- ocr
# Optional. Needs the Tesseract binary too -- see "Enabling OCR" in the
# README. Without both, OCR endpoints return 503 with install instructions and
# nothing else is affected.
pytesseract>=0.3.10

# ------------------------------------------------------------------- tests
pytest>=7.0.0
httpx>=0.27
